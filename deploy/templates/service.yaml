apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-normalization-api.fullname" . }}
  labels:
    {{- include "node-normalization-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containers.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "node-normalization-api.selectorLabels" . | nindent 4 }}
